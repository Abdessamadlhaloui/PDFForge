# =============================================================================
# PDFForge — Python Dependencies (Pinned Versions)
# =============================================================================
# Install: pip install -r requirements.txt
# =============================================================================

# --- Web Framework ----------------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0
gunicorn==23.0.0
python-multipart==0.0.19
pydantic==2.10.4
pydantic-settings==2.7.1

# --- PDF Processing ---------------------------------------------------------
PyMuPDF==1.25.3
pypdf==5.2.0
pdfplumber==0.11.6
reportlab==4.2.5

# --- Image Processing -------------------------------------------------------
Pillow==11.1.0
opencv-python-headless==4.10.0.84

# --- OCR --------------------------------------------------------------------
pytesseract==0.3.13

# --- Conversion (LibreOffice called via subprocess) -------------------------
# No Python package needed — LibreOffice installed at system level

# --- Compression (Ghostscript called via subprocess) ------------------------
# No Python package needed — Ghostscript installed at system level

# --- Background Tasks -------------------------------------------------------
celery[redis]==5.4.0
redis==5.2.1

# --- AI Features (Phase 3 — install separately if ENABLE_AI_FEATURES=true) --
transformers==4.47.1
sentence-transformers==3.3.1
torch==2.5.1
faiss-cpu==1.9.0.post1
langchain==0.3.14
langchain-community==0.3.14
langchain-huggingface==0.1.2

# --- Utilities --------------------------------------------------------------
structlog==24.4.0
httpx==0.28.1
python-dotenv==1.0.1
aiofiles==24.1.0

# --- Security ---------------------------------------------------------------
python-magic==0.4.27

# --- Development (not for production) ----------------------------------------
# pytest==8.3.4
# pytest-asyncio==0.25.0
# httpx==0.28.1
# ruff==0.8.6
# mypy==1.14.1
